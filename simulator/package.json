{
  "name": "flood-station-simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Node.js 24 simulator that seeds Santa Marta weather stations in FROST-Server and streams precipitation/temperature/humidity observations, with a control panel to trigger a 2-minute storm event.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run --exclude tests/**/*.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "fastify": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
